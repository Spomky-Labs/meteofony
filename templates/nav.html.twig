
<nav id="header" class="bg-white fixed w-full z-20 top-0 left-0 border-b border-gray-200">
    <div id="navAction" class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="{{ path('app_homepage') }}" class="flex items-center text-gray-800">
            <i class="fa-solid fa-cloud-sun"></i>
            &nbsp;
            <span class="self-center text-2xl font-semibold whitespace-nowrap">Météofony</span>
        </a>
        <div class="flex md:order-2">
            {% if is_granted('ROLE_USER') %}
                <form action="{{ logout_path('main') }}" method="post">
                    <button class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center mr-3 md:mr-0">
                        Logout
                    </button>
                </form>
            {% else %}
                <a href="{{ path('app_login') }}" type="button" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center mr-3 md:mr-0">
                    Login
                </a>
            {% endif %}
        </div>
        <div id="nav-content" class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-cta">
            <ul class="flex flex-col font-medium p-4 md:p-0 mt-4 border border-gray-100 rounded-lg bg-gray-50 md:flex-row md:space-x-8 md:mt-0 md:border-0 md:bg-white">
                {% if is_granted('ROLE_USER') %}
                    <li>
                        <a href="{{ path('app_profile') }}" class="block py-2 pl-3 pr-4 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0" aria-current="page">
                            Profile
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_dashboard') }}" class="block py-2 pl-3 pr-4 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0" aria-current="page">
                            Dashboard
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('api_doc') }}" class="block py-2 pl-3 pr-4 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0" aria-current="page">
                            API
                        </a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li>
                        <a href="{{ path('app_admin') }}" class="block py-2 pl-3 pr-4 text-white bg-blue-700 rounded md:bg-transparent md:text-blue-700 md:p-0" aria-current="page">
                            Admin
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
